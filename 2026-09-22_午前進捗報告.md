# ケア・リンク (Care-Link) システム 午前進捗報告書

**作成日時**: 2026年9月22日 (火) 11:10  
**対象システム**: 施設統合AIシステム「ケア・リンク」  
**作業範囲**: Gemini Live 双方向音声対話、GitHub Pages ゲートウェイ同期、端末マニュアル整備、DBクリーンアップ

---

## 1. 本日午前の主な成果と進捗サマリー

本日の午前セッションでは、**① Gemini Live 音声対話における自己反響（エコー）ループの解消とストリーミング細切れ保存の防止**、**② GitHub Pages CDN キャッシュ遅延を完全回避するゲートウェイ高速リダイレクト機構の刷新**、**③ 101号室居室端末（user_tablet_1）とスタッフ端末のマニュアル整合性整備** を完了いたしました。

すべての変更は Git リポジトリ（`origin/main`）に同期済みであり、安全な状態となっています。

---

## 2. 本日午前に達成された重要項目

### ① Gemini Live 音声対話の自己反響（エコー）ループ防止＆DB保存の適正化
- **音声ループの根本対策（クーリングガード導入）**:
  - AIの発話を端末スピーカーから再生した際、端末マイクがその音声を拾ってVAD（音声検知）が即座に起動し、AIが自己の挨拶を無限に繰り返してしまう現象を特定。
  - AI発話再生完了後 **1.2秒間の音声送信待機ガード（クーリング期間）** を `frontend/user/app.js` に実装。エコーの誤検知を完全遮断。
- **無音・ノイズ判定時の誤送信抑制**:
  - 無音・雑音時に不要な `EOS（発話終了）` および `turnComplete` を Gemini API へ送信しないよう条件を厳格化。
- **ストリーミングテキストの細切れDB保存防止**:
  - 1文字〜数文字単位でストリーミング配信される Gemini の応答テキストをメモリ上でバッファリングし、**1ターン完了（turnComplete）時にまとまった発話文として1件のみDB保存** するよう `backend/main.py` を改修。
  - 過去ログで小刻みに分割保存されていた重複挨拶レコードをDBからクリーンアップ。

### ② GitHub Pages ゲートウェイのCDNキャッシュ遅延解消
- **課題**:
  - GitHub Pages 経由のゲートウェイ（`docs/user/index.html`、`docs/staff/index.html`、`docs/barber/index.html`）において、GitHub Pages の CDN キャッシュ（最大10分）により最新のトンネルURL反映が遅延する課題があった。
- **解決策**:
  - キャッシュバスター（クエリパラメータ `?t=タイムスタンプ`）を付与した **`raw.githubusercontent.com` からの即時取得を最優先** するフォールバック構成に刷新。
  - トンネル再起動直後でも待ち時間ゼロで最新の接続先へ即座にリダイレクトされる機構を確立。

### ③ スタッフ端末・居室端末のマニュアル体系整備
- **端末IDの整合性同期**:
  - 101号室の居室端末IDを `user_tablet_1` として定義を統一。
  - `manuals/05マニュアル.md` の端末対照表を更新。
- **スタッフ端末用マニュアルの新規作成**:
  - `manuals/10_スタッフ端末用マニュアル.md` を新規作成し、スタッフステーションでの操作手順を体系化。
  - `manuals/README.md` の目次へ追加。

---

## 3. 現在の稼働状態と安全停止状況

午前セッション終了時点での各プロセスの稼働状態です。

| コンポーネント | 状態 | 接続先 / PID | 備考 |
| :--- | :---: | :--- | :--- |
| **バックエンド (FastAPI / Uvicorn)** | 🟢 稼働中 | `http://localhost:8000` (PID: 24346) | 正常応答確認済み |
| **Cloudflare トンネル** | 🟢 稼働中 | `https://bishop-quarterly-outcomes-rack.trycloudflare.com` (PID: 17378) | 外部公開中 |
| **Git ワーキングツリー** | 🟢 清潔 | `main` ブランチ（未コミット変更なし） | 全コードがリモートと同期済み |

---

## 4. 再起動および午後再開の手順

### 【PC本体を再起動する場合】
1. **現在のプロセスの停止（安全停止コマンド）**:
   ```bash
   pkill -f "uvicorn backend.main:app"
   pkill -f "cloudflared tunnel"
   ```
2. PCを再起動してください。

---

### 【午後作業再開時のワンライナー起動】
PC起動後、ターミナルでリポジトリ直下に移動して以下を実行するだけで、バックエンドとトンネルが自動起動し、GitHub Pages への同期まで完了します。

```bash
cd /media/k3and4/For_AI_Data/Antigravity/TestApp/Nursing_facility
./start_all.sh
```

- **居室端末（スマホ・タブレット等）アクセスURL**:  
  `https://k3and4ai-sudo.github.io/Nursing_facility/user/`
- **介護スタッフ管理画面（PCブラウザ）**:  
  `http://localhost:8000/staff/`
- **ご家族ポータル**:  
  `https://k3and4ai-sudo.github.io/Nursing_facility/`
