# デバッグ手順および開発サーバー起動ガイド

ここまでの会話内容をまとめたガイドドキュメントです。

---

## 1. デバッグを開始する手順 (VS Code)

現在の設定 ([.vscode/launch.json](file:///media/k3and4/For_AI_Data/Antigravity/TestApp/Nursing_facility/.vscode/launch.json)) では、`http://localhost:8080` に対する Chrome デバッグが設定されています。

### 手順

1. **事前準備（ローカルサーバーの起動）**
   - デバッグ対象のWebサーバーが動作している必要があります。（下記「開発サーバーの起動手順」を参照）
2. **ブレークポイントの設定**
   - デバッグしたいコードファイル（JavaScript など）を開きます。
   - 止めたい行番号の左側（余白領域）をクリックし、**赤丸（ブレークポイント）** を設定します。
3. **デバッグの実行**
   - サイドバーの **「実行とデバッグ」アイコン**（`Ctrl + Shift + D`）をクリックします。
   - 画面上部のドロップダウンで **「localhost に対して Chrome を起動する」** が選択されていることを確認します。
   - **`F5` キーを押す** か、緑色の再生ボタン（▶）をクリックしてデバッグを開始します。

### 💡 デバッグ中の操作キー
- **`F5`**: 処理の続行 (Continue)
- **`F10`**: ステップオーバー (次の行へ進む)
- **`F11`**: ステップイン (関数の中に入る)
- **`Shift + F11`**: ステップアウト (関数の外に出る)
- **`Shift + F5`**: デバッグの停止

---

## 2. 開発サーバーを事前起動する手順

### ① ターミナルの開き方
VS Code 上部メニューの **「ターミナル」 → 「新しいターミナル」**（ショートカット: `Ctrl + Shift + @` または `Ctrl + ~`）をクリックします。

### ② バックエンド API サーバーの起動 (Port 8000)
Python (FastAPI) サーバーを起動します。

```bash
# 仮想環境の有効化
source .venv/bin/activate

# サーバー起動
uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload
```

- **API ドキュメント:** `http://localhost:8000/docs`
- **スタッフ画面:** `http://localhost:8000/staff/`
- **利用者画面:** `http://localhost:8000/user/`

### ③ フロントエンド静的サーバーの起動 (Port 8080)
`.vscode/launch.json` の設定（Port 8080）に合わせてフロントエンド単体で起動する場合、別のターミナルタブで実行します。

**Python を使う場合:**
```bash
python3 -m http.server 8080 --directory frontend
```

**npx (Node.js) を使う場合:**
```bash
npx http-server frontend -p 8080
```

---

## 3. おすすめの全体手順まとめ

1. ターミナルで **バックエンドサーバー** (`uvicorn ...`) を起動する。
2. 別のターミナルタブで **フロントエンドサーバー** (`python3 -m http.server 8080 ...`) を起動する。
3. デバッグしたいコードに **ブレークポイント** を置く。
4. **`F5` キー** を押して Chrome デバッグを開始する。
