# ケア・リンク (Care-Link) システム 本日進捗報告書

**作成日時**: 2026年9月11日 (金) 17:20  
**対象システム**: 施設統合AIシステム「ケア・リンク」  
**作業範囲**: 居室端末（User）、ご家族見守りポータル（Family）、スタッフ管理ステーション（Staff）、シグナリングバックエンド（FastAPI / WebSocket）

---

## 1. 本日の主な対応項目と達成内容

### ① 居室端末（/user/）のレスポンシブ・全画面スケーリング最適化
- **画面全体の自動スケーリング**:
  - スマートフォン（縦向き・横向き）、タブレット（iPad等）、デスクトップPCの全画面アスペクト比に対応。
  - 画面からはみ出さず、スクロール不要で「話すボタン」や「アバター」「モーダル」が最適配置されるレスポンシブレイアウトを実装。

### ② ご家族ポータルとの外部接続・トンネル自動化
- **トンネルURLの自動保存 & GitHub Pages連携**:
  - `start_tunnel.sh` により、Cloudflareトンネル起動時に発行された公開URLを `docs/tunnel_url.json` へ自動書き込み＆Git Push。
  - GitHub Pages（`https://k3and4ai-sudo.github.io/Nursing_facility/`）で開いたご家族ポータルが、自動的に最新のトンネルURLを取得して接続する仕組みを確立。
- **施設サーバー稼働状態のリアルタイム診断表示**:
  - ご家族ポータル画面上部に「施設サーバー（手元PC）の起動状態（オンライン／オフライン）」および接続先URLを表示する診断バーを追加。

### ③ インターホン呼出・通話フローの改善と不具合修正
- **受話前音声遮断 & 受話操作の必須化**:
  - 居室側で「でる」ボタンを押す（または設定時間経過する）まで、ご家族やスタッフ側の音声が居室スピーカーから流れないようガード。
- **デフォルトカウントダウン時間の変更**:
  - 自動で「でる」までのデフォルト時間を **10秒 ➔ 15秒** に変更（DB、マイグレーション、API、スタッフ管理UI、居室端末UIすべて更新済み）。
- **通話状態への遷移安定化**:
  - タッチデバイスでのタップ漏れを防止するため、`touchend` / `click` の両イベントをサポート。
  - サーバー側でも15秒経過時に自動で `call_answered` を同期するダブルセーフティ機構を導入。
- **通話開始後の「でる」ボタン消去徹底**:
  - 通話開始後は CSS（`.call-card.in-call #answer-btn`）および JS の両面で「でる」ボタンを完全に消し、赤の「切る」ボタンのみを表示。
- **5秒間隔のコール音（チャイム）ループ**:
  - 呼出着信時、最初の1回だけでなく、出るまでの間 **5秒間隔でチャイム音を繰り返し再生**。
  - 受話（通話開始）または切断時に即座に停止するよう実装。

---

## 2. 動作検証ステータス

| 機能・画面 | 検証内容 | 結果 |
| :--- | :--- | :---: |
| **居室端末 (/user/)** | 各種端末画面サイズでのフィット表示（はみ出しなし） | ✅ 完了 |
| **ご家族ポータル (/family/)** | サーバー稼働状態バッジ & 自動接続診断 | ✅ 完了 |
| **GitHub Pages (docs/)** | トンネルURL自動読み込み & 外部接続 | ✅ 完了 |
| **インターホン呼出** | 15秒カウントダウン表示 & 5秒間隔チャイム再生 | ✅ 完了 |
| **インターホン受話** | 「でる」押下または15秒経過で「切る」のみになり通話開始 | ✅ 完了 |
| **インターホン切断** | 「切る」押下で双方が待機状態に正常復帰 | ✅ 完了 |

---

## 3. 次回への申し送り・確認事項
- 実機タブレットおよびスマートフォンでの実環境最終動作確認。
- 訪問理美容システム、AI見守り記録等の追加機能運用テスト。
