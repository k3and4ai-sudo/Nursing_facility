---
title: "介護施設統合AIシステム「ケア・リンク (Care-Link)」要求定義書 兼 要件仕様書 (SRS)"
date: 2026-09-03
type: specification
status: completed
project: Nursing_facility_Care-Link
tags:
  - 介護施設統合AI
  - Care-Link
  - 要件定義書
  - 要件仕様書
  - SRS
  - システム設計
  - ローカルLLM
  - Ollama
  - Gemma
  - GeminiLive
  - FullDuplex
  - AutoHealing
  - VAD
  - ファイアウォール
  - 音声AI
  - バイタルサイン管理
  - プロンプト雛形
  - 個人情報保護
  - セキュリティ
  - Progress20260903
  - Obsidian
aliases:
  - Care-Link_SRS
  - ケアリンク仕様書
  - NursingFacility_SRS
---

# 介護施設統合AIシステム「ケア・リンク (Care-Link)」
# 要求定義書 兼 要件仕様書 (System Requirements Specification: SRS)

---

> [!IMPORTANT]
> **【重要：データ安全性の最優先規定】**
> 本システムにおけるバイタルサイン抽出・判定、正式な本人確認、申し送り作成、および長期記憶（RAG）の機密データ処理は、**施設内のLinux (Ubuntu) サーバーPC上で動作するローカルLLM（Ollama等）**で実行されます。
> ただし、**利用者（被介護者）の一般会話（日常会話）のみ、外部の二重会話型AI（Full-Duplex Conversational AI / Gemini-Live）とリアルタイム音声会話を行います**。クラウド通信時も実名（本名）は一切送信せず端末判定ニックネームのみを使用し、有料版契約 (Paid Tier) によりデータ再学習・外部流出を防止することで最高度の安全性を確保します。

---

## 第1部: 要求定義書 (Business & User Requirements)

### 1.1 システム開発の背景と目的
日本の介護現場においては、高齢化に伴う要介護者の増加と介護スタッフ不足が深刻な課題となっています。特に認知症を有する利用者への丁寧な傾聴や、日々のバイタル・状態変化の細やかな記録、スタッフ間の確実な情報共有、ご家族へのこまめな連携は大きな業務負担となっています。

本システム「ケア・リンク (Care-Link)」は、**二重会話型AI (Full-Duplex Conversational AI)**、**施設内ローカルAI (Ollama)**、**音声入出力 (Whisper / gTTS)**、**マルチメディア生成 (画像/音楽/動画)**、および**暗号化通信・データベース**を統合し、以下の目的を達成します。

1. **二重会話型AI（Full-Duplex Conversational AI）による自然な会話で利用者に安らぎや安心感を与える**: 超低遅延な双方向音声対話により、認知症・高齢の利用者にストレスのない自然な傾聴・共感応答を提供。
2. **会話を通じて利用者のその日の感情を把握する**: 日常会話から機嫌・不安・不穏などの感情トーンを即時に検知・可視化し、スタッフの早期ケアに活用。
3. **会話内容を画像、動画、音楽に変換して利用者の状態を把握する**: 対話エピソードをデジタル絵手紙イラスト・安らぎBGM・思い出ショート動画に自動変換し、ご家族やスタッフが直感的に利用者の様子を把握。
4. **完全ローカルAIと匿名化による高度な個人情報保護**: バイタル・医療記録やDB暗号化は施設内ローカルLLMで完結し、クラウド会話時も実名は非開示（ニックネーム運用）としプライバシーを徹底保護。
5. **スタッフの業務負担軽減**: 音声自動認識によるバイタルサインの自動解析・記録、リアルタイム対話監視・割り込み、申し送り・チャット機能による業務効率化。
6. **ご家族および外部関係者（訪問理美容等）との安全な連携**: グループ境界アクセス制御、状態・バイタル・生成動画のリアルタイム共有、施術注意点の把握。

### 1.2 クライアント端末とモード切り替えコンセプト
クライアント端末（タブレット / スマートフォン / Webブラウザ）からシステムにアクセスし、**ユーザーコード (user_code)** でログインすると、データベースに事前登録されたロール（ユーザー区分）を取得し、自動的に最適な動作モードへ表示・機能を切り替えます。

```text
                               ┌─> 【① 利用者モード (patient)】       (大文字・シンプル操作・音声対話・自動バイタル)
                               ├─> 【② 介護スタッフモード (staff)】   (統合ダッシュボード・緊急アラート・AI割り込み・インターホン)
[ログイン (user_code認証)] ───┼─> 【③ ご家族モード (family)】         (グループ内限定・バイタル閲覧・様子共有)
                               └─> 【④ 訪問理容・美容師モード (barber)】(注意点確認・施術報告・予約管理)
```

### 1.3 グループ単位のセキュリティ境界
システム内では **「利用者 1名 - 担当介護スタッフ - ご家族」** を1つの限定グループ (`groups`) として構成します。
ご家族アカウントは**自身が所属するグループ内のみ**にアクセスが制限され、他の利用者のプライバシー情報や施設全体のデータへアクセスすることはできません。

---

## 第2部: 要件仕様書 (System Requirements Specification - SRS)

### 2.1 全体システム構成図 (System Architecture)

```text
┌─────────────────────────────────────────────────────────────────────────────────────────┐
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│                          【クライアント端末群 (Webブラウザ)】                           │
│                                                                                         │
│  ┌───────────────────────────────────────────────────────────────────────────────────┐  │
│  │                    Care-Link フロントエンド Web アプリケーション                    │  │
│  │  ┌───────────────┐ ┌───────────────┐ ┌───────────────┐ ┌────────────────────────┐  │  │
│  │  │ ① 利用者モード │ │② スタッフモード│ │ ③ ご家族モード│ │ ④ 訪問理容・美容師モード│  │  │
│  │  └───────────────┘ └───────────────┘ └───────────────┘ └────────────────────────┘  │  │
│  └──────────────────────────────────────┬────────────────────────────────────────────┘  │
│                                         │ (施設内LAN / WebSocket / HTTP REST)           │
└─────────────────────────────────────────┼───────────────────────────────────────────────┘
                                          │
                                          ▼
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│              【施設内 Linux (Ubuntu) サーバーPC - 施設内閉域AI処理基盤】                │
│                                                                                         │
│  ┌───────────────────────────────────────────────────────────────────────────────────┐  │
│  │  FastAPI (Python 3.12) - バックエンド API / WebSocket 中継 / ロール認証          │  │
│  ├───────────────────┬───────────────────┬───────────────────┬───────────────────────┤  │
│  │  音声処理基盤      │ 🔒 ローカルLLM    │ 🗄️ SQLite 3 DB     │ 🧠 ローカル RAG      │  │
│  │  (Whisper STT /   │ (Ollama Gemma 4   │ (Fernet AES-128   │ (ベクトル埋め込み     │  │
│  │   gTTS TTS)       │  26b)             │  個人情報暗号化)  │  会話長期記憶)        │  │
│  │  [施設内完結]      │ [100% 施設内完結] │ [施設内保存]      │ [施設内完結]          │  │
│  └───────────────────┴───────────────────┴───────────────────┴───────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────────────────┘
```

---

### 2.2 ローカルAI & 音声処理基盤仕様

#### 1. ローカルLLM (Ollama / Gemma 4 26b) 運用規定
- **推論環境**: サーバーPC内部に構築した **Ollama サーバー** 上で `gemma4:26b` モデルを常駐・起動。
- **正式な本人確認 & 完全オンプレミス処理**: 
  - 暗号化DB内の実名マスターとの照合・**正式な本人確認**はすべてローカルLLM側で実行。
  - 会話応答生成、感情分析、バイタルサイン抽出、会話要約、申し送り連携はすべてサーバーPCのGPU上でローカル推論を実行。外部クラウドAPIへの送信は0%。
- **認知症対応プロンプト生成**:
  - 利用者の認知症レベル (`none`, `mild`, `moderate`, `severe`) に応じてシステムプロンプトを動的生成。
  - 受容・共感の姿勢を徹底し、言葉を否定せず、安心感のある優しい口調で簡潔に応答。
  - 初回発話時のみ名前を呼び、継続対話では不自然な名前の連呼を防止する自動フィルタ処理を実施。

#### 2. Gemini-Live 連携時の名前制御・ペルソナ設定・双方向ストリーミング & セッション自動復旧仕様
- **実名の非使用 (端末判定ニックネーム運用)**:
  - Gemini-Live（クラウドリアルタイム音声対話）を利用する場合、クラウド上へ実名（本名）を送信することを**完全禁止**。
  - 接続中の端末識別子 (`terminal_id`) に紐づく**「ニックネーム（愛称）」**（例: 「なっちゃん」「たろさん」等）を使用して対話を行います。
- **ニックネーム連呼防止**:
  - Gemini-Live対話時もニックネームの連呼を厳しく抑制し、挨拶や自然なタイミングのみで使用します。
- **⚡ 完全全二重（Full-Duplex）ストリーミング & 超低遅延 VAD**:
  - 再生中ミュートを廃帰し、マイクON中は 16kHz Raw PCM パケットを Gemini Live WebSocket へ完全全二重で常時連続送出。
  - **Web Audio RMS 音量 VAD (感度しきい値 `0.0015`)**: 発話開始を 0ms 即時判定（🟢 `送信中` ランプ点灯）、150ms のポーズ検知で 🧠 `Gemini考え中...` ランプ点灯 ＋ 発話完了（`turnComplete: true`）を即時送信。
- **🔄 全自動自己修復（Auto-Healing）セッション維持機構**:
  - Googleサーバー側のWebSocket切断時、バックグラウンドで 0.3 秒以内に全自動再接続 (`_auto_reconnect`) とコンテキスト初期化 (`setup_frame`) を完了させ、常時「ホットスタンバイ状態」を自動維持。
  - 再接続中に送信されたPCMパケットはメモリキュー (`pending_chunks`) に保持され、接続完了直後に自動一括送出（パケットロスゼロ保証）。
  - 発話完了時はテキストの有無にかかわらず無条件で `turnComplete: true` を送信し、AIのデッドロック・返答途切れを完全防止。
- **🧠 直近会話コンテキスト自動引き継ぎ (Context Sync)**:
  - `setup` フレームの `systemInstruction` 内へ直近の対話履歴（最大6ターン）を注入し、接続再確立時も文脈をスムーズに継承。
- **スタッフカスタマイズ可能なロール & ペルソナ設定機能（プロンプト雛形ライブラリ搭載）**:
  - 介護スタッフはダッシュボード画面から、利用者ごとの好み・認知症の進行度・生活歴に合わせて **Gemini-Live のロール・ペルソナ（性格・話し方・トーン・指示文）を自由に設定・切り替え** が可能です。
  - 現場スタッフがプロンプト作成に迷わないよう、ワンタップで読み込める**「標準プロンプト雛形（テンプレート集）」**を標準搭載します。
  - **プロンプト雛形ライブラリ**:
    - 🌸 **雛形1: 受容・傾聴テンプレート (認知症・不穏対応)**
      > 「あなたは優しく落ち着いた介護スタッフです。利用者の話を途中で遮らず、すべて肯定的に『そうなんですね』『お気持ち分かりますよ』と受け止めてください。否定や訂正は一切せず、安心感を与える対話を行ってください。」
    - 📻 **雛形2: 回想療法・昔話テンプレート (昭和レトロ)**
      > 「あなたは昭和の時代や昔の暮らしに詳しい温かい話し相手です。『昔はどんなお仕事をされていたのですか？』『故郷の美味しい食べ物は何でしたか？』など、利用者が嬉しそうに語れる思い出を優しく引き出してください。」
    - ☀️ **雛形3: 意欲向上・アクティビティテンプレート (運動・散歩案内)**
      > 「あなたは明るく元気な健康アドバイザーです。今日の体調を気遣いながら、『今日はお天気が良いので少しお庭を歩きませんか？』とお話しし、散歩や運動・水分補給を優しく前向きに促してください。」
    - 🌇 **雛形4: 夕暮れ症候群・帰宅願望対応テンプレート (不安軽減)**
      > 「あなたは安心感を提供する見守り手です。『家に帰りたい』という訴えに『帰れません』と否定せず、『心配ですね。もうすぐスタッフがお茶を持ってきますから、少しここでお話しして待ちましょうね』と気持ちを受け止めて落ち着かせてください。」
- **役割分担の明確化**:
  - **ローカルLLM (Ollama)**: 暗号化DB照合による**正式な本人確認**・バイタルサイン記録・個人情報保護・緊急アラートを担当。
  - **Gemini-Live**: 超低遅延な日常対話・スタッフ設定ペルソナに基づく傾聴・感情豊かなコミュニケーションを担当。

#### 3. 音声処理基盤 (Whisper & gTTS)
- **音声認識 (STT)**: クライアントから送信された Base64 音声データをローカルの **Whisper (`tiny` / `base` モデル)** で即座にテキスト変換。
- **音声合成 (TTS)**: AIの応答テキストまたはスタッフの割り込みメッセージを **gTTS (Google Text-to-Speech)** により音声ファイル（MP3/WAV）に変換し、Base64 でクライアントへ送信して自動再生。

#### 4. ローカル長期記憶 (RAG) システム
- 対話履歴から重要な会話要素を抽出・ベクトル化（Embedding）し、SQLite3 データベース (`memories` テーブル) 内へ暗号化保存。
- 会話時に過去の記憶コンテキストを動的に検索・付加することで、一貫性のある対話を実現。

#### 5. 会話内容からのマルチメディア（画像・BGM音楽・ショート動画）自動生成仕様
- **① 画像生成 (Image Generation)**:
  - 利用者の発話内容（「故郷の山々」「お孫さんと過ごした日」等）からキーワードと感情を抽出し、温かみのある水彩画風・昭和ノスタルジー風の「絵手紙イラスト」を自動生成（Google Imagen 3 API / ローカルSD / 軽量Canvasテンプレート合成）。
- **② BGM音楽生成 (Background Music Generation)**:
  - 会話のムード（昭和歌謡風、のどかな和風アンビエント、穏やかなクラシック調など）に合わせ、安らぎを与える15〜30秒のバックグラウンド音楽（BGM）を自動生成。
  - 会話中のうっすらとした背景音、または生成された思い出カード・ショート動画のBGMとして再生。
- **③ ショート動画・ムービー生成 (Short Video / Animation Generation)**:
  - 生成された「イラスト画像」に優しいカメラズームパン（Ken Burns効果）を施し、「BGM音楽」および「会話の温かい要約字幕」を合成した15〜30秒の「思い出ショート動画 (MP4)」を自動作成（FFmpeg / Canvas-WebAudioエクスポート / 映像生成API）。
  - ご家族モード（アプリ/LINE）へ「今週の〇〇さんの様子ムービー」として定期送信、または施設内モニターで上映。

#### 6. スマートウォッチ連携 & バイタル・SOS検知 (BLE / Smartwatch Integration)
- **生体データ自動取得**: スマートウォッチ（BLE: Bluetooth Low Energy 通信）と連携し、心拍数・血中酸素濃度 (SpO2) を定期的に自動測定・記録。
- **転倒・緊急SOS検知**: 転倒検知センサーまたは緊急SOSボタン押下時、即座にスタッフステーションへ緊急赤フラッシュアラート (`vital_alert`) を通知。

#### 7. Google カレンダー連携 (Google Calendar API Integration)
- **服薬・予定リマインド**: 服薬時刻や通院・リハビリ予定時刻に、利用者端末からAIが音声で優しく案内（服薬リマインド）。
- **ご家族面会予約**: ご家族モード画面から Google カレンダーの空き日時を確認し、面会予約を送信・自動登録。

#### 8. 施設外メール同期 & AES-256-GCM 暗号化パケット送信エンジン
- **SMTP/IMAP メール同期エンジン**: 施設内サーバーPC上にメール同期エンジンを搭載し、グループ登録メールアドレス宛に施設外からメールリクエスト・同期処理を実施。
- **AES-256-GCM 通信パケット暗号化**: 施設外のご家族アプリへデータを送信する際、クライアント固有の暗号化キーを用いて **AES-256-GCM** でパケット全体を暗号化。アプリ側で安全に復号化して画面表示。

---

### 2.3 ユーザー区分別 4つのモード詳細仕様

| モード名 | ロール名 (`role`) | 対象ユーザー | 主な機能とUI特徴 |
|---|---|---|---|
| **① 利用者モード** | `patient` | 入居者・要介護者 | ・超シンプルUI（視認性の高い大文字 32px以上・高コントラストデザイン）<br>・マイクボタン1タップでの音声対話 & バイタル自動抽出<br>・Googleカレンダー連動による服薬・予定の音声リマインド案内<br>・スマートウォッチ(BLE)心拍/SpO2/転倒SOS自動検知<br>・異常値検出時の緊急スタッフ通知<br>・インターホン自動応答（Pattern A: 2秒後自動応答通話） |
| **② 介護スタッフモード** | `staff` | 介護士・看護師・管理者 | ・統合ダッシュボード（バイタル一覧・赤フラッシュアラート・1日会話サマリー）<br>・リアルタイム会話監視 & AI割り込み発言（Pattern B）<br>・双方向インターホン発信（Pattern A）<br>・ペルソナ & プロンプト雛形設定・ニックネーム管理<br>・申し送りノート管理 (`handover_notes`) & スタッフチャット (`staff_messages`)<br>・利用者マスター登録・端末ID紐付け・グループ管理 |
| **③ ご家族モード** | `family` | 利用者のご家族 | ・グループ内限定アクセスコントロール (`group_id` による厳格なアクセス遮断)<br>・対象利用者の本日のバイタル（体温・血圧・体重・食事量）閲覧<br>・本人の様子・AI対話の雰囲気共有 & AI生成ショート動画受領<br>・面会予約機能 (Google カレンダー連動)<br>・暗号化データ同期 (AES-256-GCM) による安全なリモート閲覧 |
| **④ 訪問理容・美容師モード** | `barber` | 訪問理美容スタッフ | ・本日の訪問理美容予約リスト表示（カット、顔剃り、カラー、パーマ）<br>・姿勢・認知症注意点（例: 「首を後ろに倒すのが困難」「洗髪時は声かけが必要」）の閲覧<br>・施術完了報告（施術内容・ご機嫌・皮膚状態）の記入とスタッフ・ご家族への共有 |

---

### 2.4 データベース & 暗号化保存仕様

#### 1. データベース構成 (SQLite3: `nursing_facility.db`)
データベースは以下の8つのテーブルで構成され、個人情報・テキストデータは **Fernet (AES-128)** で暗号化されて保存されます。

```sql
-- 1. 利用者マスター
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,                -- 【暗号化】氏名
    age INTEGER,                       -- 年齢
    room_number TEXT,                  -- 居室番号
    terminal_id TEXT UNIQUE,           -- 端末識別ID (1対1紐付け)
    dementia_level TEXT,               -- 認知症レベル ('none', 'mild', 'moderate', 'severe')
    notes TEXT,                        -- 【暗号化】申し送り・特記事項
    attention_points TEXT              -- 【暗号化】AI対話時の注意点
);

-- 2. バイタルサイン記録
CREATE TABLE vital_records (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    timestamp TEXT NOT NULL,
    temperature REAL,                  -- 体温
    weight REAL,                       -- 体重
    bp_sys INTEGER,                    -- 収縮期血圧 (上)
    bp_dia INTEGER,                    -- 拡張期血圧 (下)
    raw_text TEXT,                     -- 【暗号化】発話テキスト
    is_alert INTEGER DEFAULT 0,        -- アラートフラグ (1: 異常あり)
    alert_reason TEXT,                 -- アラート理由
    FOREIGN KEY (user_id) REFERENCES users (id) ON DELETE CASCADE
);

-- 3. 対話履歴 (短期・インターフェース用)
CREATE TABLE chat_history (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    timestamp TEXT NOT NULL,
    sender TEXT NOT NULL,              -- 'user', 'ai', 'staff'
    message TEXT NOT NULL,             -- 【暗号化】メッセージ本文
    FOREIGN KEY (user_id) REFERENCES users (id) ON DELETE CASCADE
);

-- 4. 長期記憶 (RAG用)
CREATE TABLE memories (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    timestamp TEXT NOT NULL,
    text_chunk TEXT NOT NULL,          -- 【暗号化】テキストチャンク
    embedding TEXT NOT NULL,           -- ベクトル配列 (JSON string)
    FOREIGN KEY (user_id) REFERENCES users (id) ON DELETE CASCADE
);

-- 5. スタッフチャット
CREATE TABLE staff_messages (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    timestamp TEXT NOT NULL,
    sender_name TEXT NOT NULL,
    message TEXT NOT NULL              -- 【暗号化】チャット本文
);

-- 6. 申し送りノート
CREATE TABLE handover_notes (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    timestamp TEXT NOT NULL,
    author TEXT NOT NULL,
    content TEXT NOT NULL              -- 【暗号化】申し送り内容
);

-- 7. セキュリティグループ
CREATE TABLE groups (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    group_name TEXT NOT NULL,
    patient_id INTEGER,
    FOREIGN KEY (patient_id) REFERENCES users (id) ON DELETE CASCADE
);

-- 8. ユーザーアカウント & 認証
CREATE TABLE user_accounts (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_code TEXT UNIQUE NOT NULL,    -- ログインID (例: staff01)
    password_hash TEXT NOT NULL,       -- SHA-256 ハッシュ化パスワード
    salt TEXT NOT NULL,                -- 暗号化用ソルト
    role TEXT NOT NULL,                -- 'patient', 'staff', 'family', 'barber'
    name TEXT NOT NULL,                -- 【暗号化】表示名
    group_id INTEGER,                  -- 所属グループID
    terminal_id TEXT,                  -- 紐付け端末ID
    FOREIGN KEY (group_id) REFERENCES groups (id) ON DELETE SET NULL
);
```

#### 2. セキュリティ & 暗号化アルゴリズム
- **データ暗号化 (DB保存時)**: Cryptography ライブラリの `Fernet` (AES-128-CBC + HMAC-SHA256) を使用。`.enc_key` よりキーを読込。
- **パスワード保護**: PBKDF2 / SHA-256 + 固有Salt によるハッシュ化保存。平文パスワードは保持しない。

---

### 2.5 通信プロトコル & WebSocket リアルタイム仕様

リアルタイムな見守り・対話・緊急対応を実現するため、WebSocket 通信プロトコルを採用しています。

#### 1. エンドポイント
- **利用者端末用**: `/ws/user/{terminal_id}`
- **スタッフステーション用**: `/ws/staff`

#### 2. リアルタイムメッセージプロトコル

| パターン | メッセージ種別 (`type`) | 送信元 ➔ 送信先 | 内容と処理 |
|---|---|---|---|
| **音声対話** | `audio_input` | 利用者 ➔ サーバー | 音声Base64データ送信 ➔ STT解析 ➔ バイタル判定 / AI応答 ➔ TTS音声返却 |
| **テキスト認識結果** | `transcription_result` | サーバー ➔ 利用者 | 利用者の音声認識結果テキストの即時表示用レスポンス |
| **応答返却** | `chat_response` | サーバー ➔ 利用者 | AI応答テキストおよび合成音声 Base64 データ |
| **リアルタイム監視** | `user_chat` | サーバー ➔ スタッフブロードキャスト | 利用者とAIの会話内容をリアルタイムでスタッフ画面に共有表示 |
| **緊急アラート** | `vital_alert` | サーバー ➔ スタッフブロードキャスト | 体温・血圧等の異常値検出時、スタッフ画面へ赤フラッシュと警告を発動 |
| **Pattern B (割り込み)** | `staff_override` | スタッフ ➔ サーバー ➔ 利用者 | スタッフがAI対答に割り込みテキスト送信 ➔ 音声合成して利用者端末で優先再生 |
| **Pattern A (通話開始)** | `call_request` | スタッフ ➔ サーバー ➔ 利用者 | スタッフから特定端末へのインターホン呼び出し通知 |
| **Pattern A (音声流し)** | `audio_stream` | 双方 ➔ サーバー ➔ 双方 | 双方向リアルタイム音声ストリーミング |
| **Pattern C (スタッフ間)** | `staff_chat` | スタッフ ➔ サーバー ➔ スタッフ全端末 | スタッフステーション間リアルタイムメッセージ |

---

### 2.6 REST API エンドポイント仕様

FastAPI バックエンドが提供する主な HTTP API エンドポイント一覧です。

```text
POST   /api/auth/login             : ユーザーコード・パスワード認証およびロール・グループ取得
GET    /api/users                  : 利用者一覧の取得
POST   /api/users                  : 新規利用者の登録
GET    /api/users/{user_id}        : 指定利用者の詳細情報取得
PUT    /api/users/{user_id}        : 指定利用者の情報更新
DELETE /api/users/{user_id}        : 指定利用者の削除
GET    /api/users/terminal/{t_id}  : 端末IDから利用者情報の取得
GET    /api/users/{user_id}/vitals : 指定利用者のバイタル記録履歴の取得
GET    /api/vitals                 : 全利用者の直近バイタル一覧の取得
GET    /api/users/{user_id}/chat   : 指定利用者の対話履歴取得
GET    /api/handovers              : 申し送りノート一覧の取得
POST   /api/handovers              : 申し送りノートの新規投稿
GET    /api/staff-messages         : スタッフ間チャット履歴の取得
```

---

### 2.7 非機能要件 (Non-Functional Requirements)

1. **ファイアウォール内設置 & ローカル閉域網分離 (Firewall & Network Isolation)**:
   - 施設内 Linux (Ubuntu) サーバーPCは、外部インターネットからの不正アクセス遮断および意図しない情報漏洩を防止するため、**ハードウェア/ソフトウェア ファイアウォール (UFW / 防火壁) の内側に配置**します。
   - 施設内LAN（ローカルWi-Fi閉域網/専用VLAN）からの特定ポート（FastAPI API/WebSocket用 8000ポート等）のみ通信を許可し、Ollama API (11134) やデータベース等の内部サービスポートはファイアウォールで厳密に遮断・非公開とします。
   - 外部インターネット接続が遮断・切断された環境下でも、施設内閉域機能（AI対話・バイタル記録・アラート発動）は100%継続動作可能です。
2. **応答低遅延性**:
   - 会話入力からAI音声応答開始までを 3秒以内 に処理（GPUローカル推論およびストリーミング設計）。
3. **自動復旧とバックエンド管理**:
   - Linux Systemd サービス登録による OS 起動時のバックエンド（FastAPI / Uvicorn）および Ollama サービスの自動起動・自動リカバリ設定。
4. **耐障害性とセキュリティ**:
   - データベース・認証暗号化キーの保護。
   - 利用者個人情報のデータベース内全件暗号化保存 (`Fernet` AES-128)。

---

### 2.8 Gemini-Live ライセンス・データプライバシー・クラウドコスト運用規定

本システムで Gemini-Live（Multimodal Live API）を安全かつ継続的に商用運用するための、APIライセンス・データ非公開（モデル再学習の禁止）・コスト管理に関する規定です。

#### 1. データ非公開規定 & モデル再学習の禁止 (Zero Data Retention & Training Opt-Out)
- **有料版ティア (Google Cloud Vertex AI / Gemini API Paid Tier) の強制適用**:
  - 無料版（Free Tier / AI Studio Free）では入力データがGoogleのモデル改善・再学習に利用される利用規約となっているため、本システムでの**使用を厳禁**とします。
  - 必ず法人契約に基づく **Google Cloud Vertex AI** または **Gemini API Paid Tier (Pay-as-you-go / 従量課金版)** を採用します。
- **データ非開示・学習利用ゼロの担保**:
  - Paid Tier 契約に基づき、利用者端末から送信される音声ストリーミングデータ、テキストデータ、生成応答は**Googleのモデルトレーニング・再学習に一切使用されません（Data Privacy Compliance / Opt-Out 完了状態）**。

#### 2. コスト管理・課金コントロール規程
- **従量課金モデル (Audio Token Pricing)**:
  - 音声ストリーミング入力および音声出力トークン単位での従量課金（時間・トークン換算）が発生します。
- **コスト上限・バジェット上限アラート**:
  - Google Cloud Console 上でプロジェクトごとの月額利用予算（Budget & Alert）を設定し、想定外の課金超過を物理的に防止します。
- **セッション時間・タイムアウト自動制御**:
  - 1回の無応答放置や長時間の会話セッションに対して上限時間（例: 連続会話 15分で自動切断・待機モード移行）をシステム側で制御し、APIトークン消費を最小限に抑えます。

---

## 第3部: 各資料統合に伴う矛盾点・懸念点および整合性の担保方針

各要求ドキュメント（「Care_Link_System_Requirements.pdf」「要件定義質疑応答.pdf」および新提案機能仕様）の統合に伴い抽出された**4つの主要な矛盾点・整合性上の課題**と、その解消・統一方針です。

### ⚠️ 矛盾点 1: 【完全ローカル閉域網・クラウド絶対遮断】 vs 【Gemini-Live & Google連携等のクラウド通信】

* **各資料の記載差**:
  - `要件定義質疑応答.pdf` / 初期SRS: 「外部API（音声認識・音声合成等）への依存を一切排除」「物理的・論理的に外部クラウドLLM APIへの通信経路を遮断し100%施設内完全完結」。
  - 新機能仕様: 利用者モードでの自然な音声会話に「Gemini-Live (Google Cloud WebSocket)」を採用。また、Google Calendar API、Google Imagen 3 API 等のクラウド連携を導入。
* **整合性の担保方針 (Gemini-Liveの会話限定適用 ＆ ハイブリッド分離モデル)**:
  - **Gemini-Liveの適用範囲限定**: **Gemini-Liveの利用は「利用者モードにおける日常のリアルタイム音声会話」のみに厳格に限定**します。クラウド通信発生時のセキュリティ対策として、前述の通り実名は一切送信せず端末判定ニックネームのみを使用します。
  - **その他AI処理の100%ローカル保証**: バイタルサイン抽出・判定、正式本人確認、感情分析、申し送り要約生成、RAG長期記憶、緊急アラート等の**その他の全AI処理はすべて施設内のローカルLLM (Ollama等) で実行**します。
  - **オフラインフォールバック**: 万が一インターネット接続が途絶した場合は、利用者会話もローカルLLM (Ollama + Whisper + gTTS) による閉域会話モードへ自動フォールバックし、施設内運用が100%継続動作する安全設計とします。

---

### ⚠️ 矛盾点 2: 【実名（本名）マスター管理・本人確認】 vs 【Gemini-Liveでの実名送信絶対禁止・端末ニックネーム運用】

* **各資料の記載差**:
  - `Care_Link_System_Requirements.pdf` / Q&A: 「利用者マスターで氏名（実名）を暗号化管理し、初回挨拶時にお名前で呼ぶ」。
  - Gemini-Live仕様: 「クラウドへの実名（本名）送信を絶対禁止とし、端末ID紐付けのニックネームのみを使用」。
* **整合性の担保方針 (名前処理の明確なレイヤー分離)**:
  - **ローカルレイヤー (Ollama / DB)**: 正確な医療・介護記録、正式な本人特定、スタッフ向けダッシュボード表示においては暗号化された「実名（本名）」を使用。
  - **クラウドライヤー (Gemini-Live)**: クラウド側へ送信するプロンプトおよびコンテキストには端末IDから動的変換した**「ニックネーム（愛称）」のみを注入**し、ローカルとクラウド間でプライバシー境界を物理的・論理的に分離する。

---

### ⚠️ 矛盾点 3: 【サーバーPCハードウェアスペック (RTX 2060 8GB) とモデルサイズ (gemma4:26b 17GB) の乖離】

* **各資料の記載差**:
  - `要件定義質疑応答.pdf` (P.2): 「Gemma 27B の快適動作には VRAM 16GB〜24GB (RTX 3090/4090等) が必要」と記載。
  - 実際のサーバーPC環境 (Q&A P.4): `nvidia-smi` の結果、搭載GPUは **NVIDIA GeForce RTX 2060 (VRAM 8GB)** であり、Ollama上のモデルは `gemma4:26b` (ファイルサイズ 17GB)。
* **懸念点 & 整合性の担保方針 (ハードウェアスペックに応じたローカルLLMの柔軟選定)**:
  - Gemma 27B / `gemma4:26b` に限定・固執せず、**実際に運用環境として準備するサーバーPCのハードウェアスペック（GPU/VRAM容量）に合わせて最適なローカルLLM（モデルサイズおよび4-bit量子化版等）を柔軟に変更・選定**する方針とします。
  - VRAM 8GB 環境 (RTX 2060等) の場合は、低遅延で快適に動作するモデル（例: `gemma2:9b` / `gemma2:2b` または量子化版）を適用し、将来的にVRAM 16-24GB以上のサーバーが導入された際に大型モデルへスムーズにアップグレードできる可変構成とします。

---

### ⚠️ 矛盾点 4: 【バイタルサイン抽出・登録確認フローの表記比較】

* **各資料の記載差**:
  - `要件定義質疑応答.pdf` (P.3 ④): 「GemmaでJSONパース後、パースしたデータを音声と文字で確認し、異常値は音声と文字で警告を出す」。
  - `Care_Link_System_Requirements.pdf`: 「マイク1タップで発話し、自動抽出してスタッフ画面へ緊急赤フラッシュアラート通知」。
* **整合性の担保方針 (確認フローの統一・一本化)**:
  - **利用者端末画面**: 発話後、画面上に「体温 36.5℃、血圧 120/80 ですね。登録します。」と**文字プレビュー表示＋音声読み上げによる最終確認**を行い、数秒後に自動確定/ワンタップ登録。
  - **異常値検出時**: 利用者端末で「体温が高めです。スタッフにお知らせしますね」と**音声＋画面警告を表示**し、同時にスタッフステーションへ**緊急赤フラッシュアラート (`vital_alert`) をブロードキャスト送信**する統合フローへ一本化。

---

## 第4部: 最新開発進捗ステータス・実装検証実績報告 (2026-09-03現在)

### 4.1 実装完了コンポーネント & 機能

| レイヤー | モジュール・コンポーネント | 実装状況・動作検証結果 |
|---|---|---|
| **バックエンド** | `backend/gemini_live.py` | ✅ **完了**: Gemini Live Bidi WebSocket (`BidiGenerateContent`) 統合、0.3s 背景 `_auto_reconnect` セッション自動復旧、`pending_chunks` キューイング、無条件 `turnComplete: true` 送信、会話履歴（最大6ターン）同期 |
| **バックエンド** | `backend/main.py` | ✅ **完了**: `/ws/user/{terminal_id}/live` エンドポイント、重複REST/Ollama応答の完全削除、`add_pcm_chunk_sync` によるイベントループ同期化、`PIIGuardrailMonitor` (3.0s間隔) |
| **フロントエンド** | `frontend/user/app.js` | ✅ **完了**: 完全全二重 PCM 16kHz ストリーミング、Web Audio RMS 音量 VAD (感度 `0.0015`)、150ms 超高速ポーズ検知、カプセル型 3色ステータスランプ連動、重複TTS削除 |
| **フロントエンド** | `frontend/user/index.html` & `style.css` | ✅ **完了**: 高輝度パルス表示 3色ステータスランプ（🟢 `送信中` / 🟡 `考え中...` / 🔵 `応答中`）、レスポンシブ大文字UI |
| **データ基盤** | `backend/database.py` & SQLite3 | ✅ **完了**: Fernet (AES-128) 個人情報暗号化、全8テーブル構築、ユーザーコードロール認証 |

### 4.2 単体テスト自動検証結果
- **テストスクリプト**: `backend/test_backend.py` および `backend/test_gemini_live.py`
- **実行結果**: **全17件の単体テスト項目 全件100%合格 (`OK`)**
  - DB暗号化/復号化、ロール判定、バイタルサイン抽出、Gemini Live BidiWebSocket初期化、setupフレーム構築、`turnComplete` 無条件送信動作検証完了。

### 4.3 稼働確認手順
- **サーバー起動**: `.venv/bin/uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload`
- **利用者モード接続URL**: `http://localhost:8000/user/`

---

*最終更新日: 2026年9月3日*  
*作成著者: Care-Link AI システム開発プロジェクトチーム*
