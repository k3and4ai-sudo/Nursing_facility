# 介護施設統合AIシステム「ケア・リンク (Care-Link)」
# 要求定義書 兼 要件仕様書 (System Requirements Specification: SRS)

---

> [!IMPORTANT]
> **【重要：データ安全性の最優先規定】**
> 本システムにおける利用者様との会話応答、感情分析、バイタルサイン抽出、および長期記憶（RAG）の全AI処理は、**施設内のLinux (Ubuntu) サーバーPC上で動作するローカルLLM（Ollama / Gemma 4 26b）のみ**で実行されます。
> 外部のクラウドAIサービス（OpenAI, Anthropic, Google Cloud等の外部LLM API）へ音声データや会話テキストを送信することは**一切ありません**。すべてのデータ処理が施設内で完全完結（オンプレミス・閉域）するため、機密性の高い要介護者の会話や個人健康情報の外部流出リスクを根本的に遮断し、最高度の安全性を確保します。

---

## 第1部: 要求定義書 (Business & User Requirements)

### 1.1 システム開発の背景と目的
日本の介護現場においては、高齢化に伴う要介護者の増加と介護スタッフ不足が深刻な課題となっています。特に認知症を有する利用者への丁寧な傾聴や、日々のバイタル・状態変化の細やかな記録、ご家族へのこまめな情報共有は大きな業務負担となっています。

本システム「ケア・リンク (Care-Link)」は、**施設内PCで完結するローカルAI（Ollama / Gemma 4 26b）**、**スマートウォッチ**、および**暗号化通信**を統合し、以下の目的を達成します。

1. **完全ローカルAIによる高度な情報保護**: 施設外へ音声・会話データを一切出力せず、ローカルLLMでプライバシーを徹底保護。
2. **スタッフの業務負担軽減**: 音声自動入力、ローカルAIバイタル解析、タスク自動生成による記録業務の短縮。
3. **利用者の安心感とQOL向上**: 認知症に特化した傾聴AI、大きな画面と音声によるリマインド、24時間見守り。
4. **ご家族への高い透明性と安心の提供**: リアルタイムな状態閲覧、面会予約、定期暗号化データ共有。

### 1.2 クライアント端末とモード切り替えコンセプト
クライアント端末（Androidスマートフォン / タブレット）には統合アプリを導入します。
アプリ起動時に**ユーザーID**でログインすると、サーバーPCに事前登録されたユーザー区分を取得し、自動的に最適な動作モードへ表示・機能を切り替えます。

```text
                               ┌─> 【利用者モード】       (大文字・超シンプル操作・機能制限)
                               ├─> 【介護スタッフモード】 (統合ダッシュボード・バイタル・割り込み)
[ログイン (ユーザーID認証)] ──┼─> 【ご家族モード】       (グループ内通信限定・暗号化更新同期)
                               └─> 【訪問理容・美容師モード】(カット予約・姿勢注意点・施術報告)
```

### 1.3 グループ単位のセキュリティ境界
システム内では **「利用者 1名 - 担当介護スタッフ - ご家族」** を1つの限定グループとして構成します。
ご家族アカウントは**自身が所属するグループ内のみ**にアクセスが制限され、他の利用者のプライバシー情報や施設全体のデータへアクセスすることはできません。

---

## 第2部: 要件仕様書 (System Requirements Specification - SRS)

### 2.1 全体システム構成図 (System Architecture)

```text
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│                           【Android クライアント端末群】                                │
│                                                                                         │
│  ┌───────────────────────────────────────────────────────────────────────────────────┐  │
│  │                    Care-Link 統合 Android アプリ (スマホ / タブレット)               │  │
│  │  ┌───────────────┐ ┌───────────────┐ ┌───────────────┐ ┌────────────────────────┐  │  │
│  │  │ 利用者モード  │ │ スタッフモード│ │ ご家族モード  │ │ 訪問理容・美容師モード │  │  │
│  │  └───────────────┘ └───────────────┘ └───────────────┘ └────────────────────────┘  │  │
│  └──────────────────────────────────────┬────────────────────────────────────────────┘  │
│                                         │ (施設内LAN / WebSocket / HTTPS)               │
└─────────────────────────────────────────┼───────────────────────────────────────────────┘
                                          │
                                          ▼
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│              【施設内 Linux (Ubuntu) サーバーPC - 施設内閉域AI処理基盤】                │
│                                                                                         │
│  ┌───────────────────────────────────────────────────────────────────────────────────┐  │
│  │  FastAPI (Python 3.12) - バックエンド / 認証・モード判定 / WebSocket中継              │  │
│  ├───────────────────┬───────────────────┬───────────────────┬───────────────────────┤  │
│  │  Local Speech Engine│ 🔒 ローカルLLM    │  SQLite Database  │  暗号化データ同期     │  │
│  │  (Whisper / gTTS)  │ (Ollama/Gemma 26b)│  (Fernet Encrypted│  (AES-256-GCM Engine) │  │
│  │  [施設内完結]      │ [100% 施設内完結] │  [施設内保存]     │  [個別に暗号化送信]   │  │
│  └───────────────────┴───────────────────┴───────────────────┴───────────────────────┘  │
└─────────────────────────────────────────┬───────────────────────────────────────────────┘
                                          │ (施設外インターネット通信)
                                          ▼
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│                       【施設外通信 (Gmail / スマートウォッチ / ご家族)】                    │
│  ┌────────────────────────┐  ┌────────────────────────┐  ┌────────────────────────┐  │
│  │  Google Calendar API   │  │  暗号化メールリクエスト │  │ スマートウォッチ (BLE) │  │
│  │  (服薬/通院/面会予約)  │  │  (施設外からのデータ更新│  │ (心拍・SpO2・転倒SOS)  │  │
│  └────────────────────────┘  └────────────────────────┘  └────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────────────────┘
```

---

### 2.2 ローカルLLM（Ollama / Gemma 4 26b）運用仕様

1. **完全ローカル推論**:
   * サーバーPC内部に構築した **Ollama サーバー** 上で **Gemma 4 26b** モデルを常駐・起動。
   * 利用者の会話に対する応答生成、会話からのバイタル数値抽出、会話要約、および感情分析は、すべてサーバーPCのGPU (NVIDIA RTX) 上でローカル推論を実行。
2. **クラウドLLM遮断ポリシー**:
   * バックエンドコード（FastAPI）から外部LLM APIへの通信経路を物理的・論理的に一切排除。
   * 万が一インターネット接続が遮断された環境であっても、AIとの自然会話機能およびバイタル記録機能は100%影響を受けずに完全動作する。
3. **長期記憶（RAG）のローカル処理**:
   * 対話履歴の埋め込みベクトル生成（Embedding）もローカルモデルで処理し、SQLiteデータベース内に暗号化保存。外部のベクトル検索クラウドサービスは不使用。

---

### 2.3 ユーザー区分別 モード仕様

#### ① 利用者モード (Patient Mode)
* **超シンプルUI**: 画面上のボタン数を削減し、視認性の高い超大文字（32px以上）と高コントラストデザインを採用。
* **ローカルAI音声対話 & バイタル抽出**: マイクボタン1タップで発話。「体温36.5度」等の数値をローカルのWhisper+Gemmaが抽出・記録。
* **認知症傾聴プロンプト**: 否定・修正を行わない安心感重視のAI応答。
* **2秒自動応答インターホン (Pattern A)**: スタッフからの呼び出し時、2秒後に自動応答してハンズフリー通話を開始。
* **Googleカレンダーリマインド**: 服薬・予定時刻に音声で優しく案内。

#### ② 介護スタッフモード (Staff Mode)
* **統合ダッシュボード**: 緊急赤フラッシュアラート、リアルタイムバイタル一覧、ローカルAIが要約した1日の会話サマリー。
* **対話監視 & 割り込み (Pattern B)**: 利用者とAIの会話をリアルタイム監視し、スタッフのテキスト/音声を割り込み送信。
* **双方向インターホン (Pattern A)**: 各部屋の端末とのリアルタイム音声通話。
* **利用者マスター & グループ管理**: 利用者の登録、端末ID紐付け、ご家族アカウントのグループバインド。
* **申し送り & スタッフチャット (Pattern C)**: シフト引継ぎノートの記録とスタッフ間リアルタイムメッセージ。

#### ③ ご家族モード (Family Mode)
* **グループ内限定通信**: 紐付けられた利用者・担当スタッフのみとメッセージ・写真・動画の送受信。
* **リアルタイム様子・バイタル確認**: 本日の体温、食事量、ローカルAIとの会話の雰囲気（ご機嫌状態）の閲覧。
* **面会予約 (Google Calendar連動)**: 空き日時を確認して面会予約を送信。

#### ④ 訪問理容・美容師モード (Visiting Barber Mode)
* **理美容予約一覧**: 本日および今後のカット、顔剃り、カラー、パーマの予約リスト表示。
* **姿勢・認知症注意点の確認**: 「首を後ろに倒すのが困難」「洗髪時は声かけが必要」等の注意点閲覧。
* **施術完了報告**: 施術内容、ご本人のご機嫌、皮膚の状態などを記入し、スタッフおよびご家族へ共有。

---

### 2.4 グループ機能 ＆ 施設外メール送信・暗号化データ同期仕様

#### 1. グループ単位のアクセス制御
* データベース上で `groups` テーブルを定義。`group_id` にて「利用者ID - 担当スタッフIDs - ご家族IDs」をバインド。
* ご家族からのすべてのAPIリクエスト/WebSocket接続に対し、サーバー側で `group_id` を検証し、自グループ外のデータアクセスを厳格に遮断。

#### 2. 登録メールアドレスによる施設外メール送受信
* グループメンバー間で登録されたメールアドレス宛に、施設外からでもメールを送受信可能。
* サーバーPC内に **SMTP/IMAP メール同期エンジン** を搭載し、定期的にメール受信・自動レスポンス処理を実施。

#### 3. ご家族からの要求に応じた暗号化データ送信 & アプリ復号化
```text
【ご家族アプリ / メール】                 【Ubuntu サーバーPC (ローカル完結)】
       │                                        │
       │ ── (1) 更新要求 / メールリクエスト ──> │
       │                                        │ ── (2) 最新データを取得 (ローカルDB)
       │                                        │ ── (3) 個別キーで暗号化 (AES-256-GCM)
       │ <── (4) 暗号化データパケット送信 ───── │
       │
       ▼
(5) ご家族アプリ内で復号化
    -> アプリ画面（バイタル・写真・日誌）へ即座に反映
```

* **暗号化仕様**:
  * サーバーPCから施設外のご家族アプリへデータを送信する際、平文の個人情報は送信せず、クライアント固有の暗号化キーを用いて **AES-256-GCM** でパケット全体を暗号化。
  * ご家族アプリ側で受信した暗号化パケットを復号化し、アプリ内の画面へ安全に反映。

---

## 2.5 非機能要件 (Non-Functional Requirements)

1. **ローカルLLM動作環境 & セキュリティ規定 (Ubuntu Linux)**:
   * **ローカルAI環境**: Ollama サーバーおよび Gemma 4 26b モデルを物理サーバー内で完結起動。外部クラウドLLM通信はゼロ。
   * **自動起動設定**: Systemd サービス登録によるOS起動時のバックエンドおよびOllamaの自動立ち上げ。
   * **GPUメモリ管理**: NVIDIA VRAM（RTX 2060以上）をローカル推論用に専用割り当て。
2. **クライアント環境 (Android)**:
   * Android 8.0 以上に対応したレスポンシブアプリ。
   * オフラインキャッシュ機能を備え、通信一時切断時も画面表示を保護。
3. **データベース＆通信暗号化管理**:
   * SQLiteデータベース内は `Fernet` (AES-128) で暗号化保存。
   * 施設外通信・メール添付パケットは `AES-256-GCM` で暗号化し、不正傍受を完全に防止。
