# ケア・リンク (Care-Link) システム 本日進捗報告書

**作成日時**: 2026年9月12日 (土) 23:25  
**対象システム**: 施設統合AIシステム「ケア・リンク」  
**作業範囲**: 居室端末（User）、スタッフ管理ステーション（Staff）、シグナリングバックエンド（FastAPI / WebSocket / DB）

---

## 1. 本日の主な対応項目と達成内容

### ① スマートウォッチ（BLE）直接連携 & Web Bluetooth API 実装
- **居室端末（/user/）の Bluetooth 連携**:
  - ブラウザ標準の **Web Bluetooth API** を用いて標準心拍サービス（`0x180D`）とワンタップでペアリングする機能を実装。
  - 受信した心拍パルス（8bit/16bit 自動判定）をリアルタイムにサーバーへ中継。
  - ヘッダーバッジ表示（`⌚ ウォッチ未接続` ➔ `⌚ ❤️ 72 bpm 🫁 98%` 等）のリアルタイム動的更新。
- **セキュリティ要件（HTTPS / セキュアコンテキスト）の整理**:
  - Web Bluetooth API がブラウザ仕様上「HTTPS または localhost」必須であることに対し、Cloudflare トンネル経由のセキュア接続環境を整備。

### ② 内蔵バイタルシミュレータ & 転倒SOS機能
- **検証用バイタルシミュレータ（モーダル内）**:
  - 実機スマートウォッチがない環境でも即座にテストできるよう、シミュレータ機能を居室画面に内蔵。
  - 🟢 正常バイタル (72bpm / 98%)
  - ⚠️ 頻脈アラート (128bpm)
  - 🫁 低酸素アラート (91%)
  - ▶️ 自動心拍パルス (5秒ごとに連続送信)
  - 🚨 転倒・緊急SOS検知発信 (スタッフ即時通知)

### ③ スタッフステーション（/staff/）のバイタル・SOS連動
- **「最近測定されたバイタル情報」テーブルへの心拍/SpO2列新設**:
  - 心拍数と血中酸素飽和度（SpO2）のリアルタイム表示を追加。
  - 頻脈・低酸素・SOS検知時の赤フラッシュ通知およびアラートサイレン音の連携を完了。

### ④ データベース & バックエンドAPI拡張
- **`vital_records` テーブルの拡張**:
  - `heart_rate INTEGER`, `spo2 INTEGER`, `source TEXT` カラムを追加（既存DBに対する自動マイグレーション対応）。
- **REST API & WebSocket ハンドラ**:
  - `POST /api/users/{user_id}/vitals`
  - `/ws/user/{terminal_id}` における `vital_data` および `emergency_sos` イベントハンドラ実装。
  - 全16件の自動単体・統合テストが全件正常通過（OK）。

---

## 2. 動作検証ステータス

| 機能・画面 | 検証内容 | 結果 |
| :--- | :--- | :---: |
| **バックエンド自動テスト** | `test_backend.py` & `test_smartwatch.py` 全16件実行 | ✅ 成功 (OK) |
| **居室端末 (/user/)** | シミュレータによる正常・異常バイタル送信 & バッジ更新 | ✅ 成功 |
| **スタッフ画面 (/staff/)** | バイタル履歴テーブルへの心拍・SpO2反映 & リアルタイム受信 | ✅ 成功 |
| **HTTPSトンネル接続** | Cloudflare トンネル経由での居室UI・APIアクセス疎通 | ✅ 成功 |

---

## 3. 次回への申し送り・確認事項
1. **実機スマートウォッチのBLEペアリング確認**:
   - HTTPSトンネル経由（またはセキュア設定済み端末）から、実機スマートウォッチとのBluetooth接続テスト。
2. **Google Fit クラウド連携（OAuth 2.0）の再開**:
   - クラウド経由での歩数・バイタル自動同期について、安全な認証手順で完了・検証を行う。
